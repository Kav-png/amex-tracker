import type { NextConfig } from "next"

const nextConfig: NextConfig = {
  // next-pwa injects the service worker at build time
  // Add PWA via @ducanh2912/next-pwa or handle via manifest + custom SW
}

export default nextConfig
